{
  "name": "portafolio-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "dev:all": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "vercel build",
    "deploy": "vercel deploy"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "vercel": "latest"
  }
}
